# --- File: docker/.env (Definitive Final Version) ---

# ===================================================================
# 1. PostgreSQL Credentials (for MLflow and Airflow Metadata)
# ===================================================================
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=airflow

# ===================================================================
# 2. Airflow Configuration
#    These are the core variables needed by the official Airflow image.
# ===================================================================


# Credentials for the Airflow webserver login
AIRFLOW_WWW_USER_USERNAME=airflow
AIRFLOW_WWW_USER_PASSWORD=airflow
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@hres_postgres:5432/${POSTGRES_DB}
AIRFLOW__CORE__EXECUTOR=LocalExecutor

# ===================================================================
# 3. Azure OpenAI Credentials (for API's Conversational AI)
#    These are your actual keys.
# ===================================================================
AZURE_OPENAI_API_KEY=vE0SsFFmCIXDOgqOlTPWK3IT2hsfPnyLnmFK0349RSte3Nzp97kWJQQJ99BCACYeBjFXJ3w3AAABACOGNjTP
AZURE_OPENAI_ENDPOINT=https://gpt-4otest.openai.azure.com/
AZURE_DEPLOYMENT_NAME=gpt-4o
OPENAI_API_VERSION=2024-12-01-preview
OPENAI_API_TYPE=azure