# --- File: docker/.env (Definitive Final Version - With Airflow Credentials) ---

# ===================================================================
# 1. PostgreSQL Credentials (for MLflow and Airflow Metadata)
# ===================================================================
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=airflow # Airflow default DB name
# MLflow will also use this database, creating its own tables.

# ===================================================================
# 2. Azure OpenAI Credentials (for API's Conversational AI)
#    These MUST be valid for the chat feature to work.
# ===================================================================
AZURE_OPENAI_ENDPOINT="https://gpt-4otest.openai.azure.com/"
AZURE_OPENAI_API_KEY="YOUR_AZURE_OPENAI_API_KEY_HERE" # <<< IMPORTANT: Replace with your actual key
OPENAI_API_VERSION="2023-12-01-preview"
AZURE_DEPLOYMENT_NAME="gpt-4o"

# ===================================================================
# 3. Airflow Configuration
#    These are defaults for Airflow's internal settings.
# ===================================================================
# Webserver and scheduler will use these credentials
AIRFLOW_WWW_USER_USERNAME=airflow
AIRFLOW_WWW_USER_PASSWORD=airflow

# This UID is important for volume permissions in Linux environments
AIRFLOW_UID=50000

# Specify executor type. CeleryExecutor is for distributed workers, LocalExecutor for single machine.
AIRFLOW_EXECUTOR=LocalExecutor
# Database connection string for Airflow
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@hres_postgres:5432/${POSTGRES_DB}
# The Airflow webserver needs to know about the MLflow tracking URI for potential UI links
AIRFLOW__CORE__MLFLOW_TRACKING_URI=http://hres_mlflow:5000

# ===================================================================
# 4. Project Metadata (Optional)
# ===================================================================
PROJECT_OWNER="Shameem"