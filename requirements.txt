# --- File: requirements.txt ---

# Core Data Science & ML
pandas==2.1.4
numpy==1.26.3
scikit-learn==1.3.2
tqdm==4.66.1
plotly==5.18.0

# MLflow for MLOps
mlflow-skinny==2.9.2
psycopg2-binary==2.9.9 # PostgreSQL driver for MLflow/Airflow backend

# API Framework & Server
Flask==3.0.0
gunicorn==21.2.0      # Production WSGI server for Flask
pydantic==2.5.3       # For robust data validation in the API
requests==2.31.0      # For making HTTP requests (used in UI and tests)

# Conversational AI (Azure)
openai==1.12.0

# User Interface
streamlit==1.30.0

# Notebook Environment
jupyterlab==4.0.10
ipykernel==6.28.0

# Testing Framework
pytest==7.4.4
pytest-cov==4.1.0

# Note: apache-airflow is installed separately in its Dockerfile
# to use a specific version with constraints for stability.